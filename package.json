{
  "name": "cat-gallery-project",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "start": "http-server -p 3000"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "http-server": "^14.1.1"
  }
}
